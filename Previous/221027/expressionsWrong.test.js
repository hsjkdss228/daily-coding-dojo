// import solution, { accumulate, makeArray } from './expressions';

// test('시작점과 자연수 n이 주어졌을 때 시작점~ceil(n/2)까지의 자연수 배열을 생성', () => {
//   expect(makeArray({ start: 4, number: 15 }))
//     .toStrictEqual([4, 5, 6, 7, 8]);
// });

// test('특정 숫자부터 자연수 ceil(n/2)까지의 합을 누산하는 과정에서 자연수 n이 도출되는지 확인', () => {
//   expect(accumulate({ start: 4, number: 15 })).toBe(true);
// });

// test('0~ceil(n/2)까지 돌면서 식이 도출된 횟수 측정', () => {
//   const number = 15;
//   expect(solution(number)).toBe(4);
// });
