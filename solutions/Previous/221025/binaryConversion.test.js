// import solution, { convert, convertUntil1 } from './binaryConversion';

// test('이진수 문자열을 변환 (0 제거, 길이 측정, 측정 길이를 2진수로 변환, 제외한 0 개수를 같이 반환', () => {
//   expect(convert('0111010')).toStrictEqual({
//     convertedBinaryString: '100', zeroExcludedCount: 3,
//   });
// });

// test('첫 주어진 이진수 문자열을 1이 될 때까지 반복해서 2진수로 변환', () => {
//   expect(convertUntil1('0111010')).toBe('1');
// });

// test('solution', () => {
//   expect(solution('110010101001')).toBe([3, 8]);
// });
